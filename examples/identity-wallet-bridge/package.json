{
  "name": "identity-wallet-bridge",
  "version": "0.1.0",
  "description": "Bridge between Identity Wallet DApp and CLI",
  "private": true,
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
} 